# Images

- **`logo.png`** – App branding (splash and auth screens).
- **`bottleImages.js`** – Resolves product image for display using API image URL only (no local bottle assets; keeps bundle size small).
